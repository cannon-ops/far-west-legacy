# Far West Legacy — Python Dependencies

# Core
flask>=3.0
gunicorn>=22.0
python-dotenv>=1.0
anthropic>=0.40
requests>=2.31
httpx>=0.27

# Obituary fetching & parsing
beautifulsoup4>=4.12
lxml>=5.0

# Resthaven Mortuary only (src/sources/resthaven_source.py) — resthavenmort.com's
# Cloudflare config 403s requests/httpx on TLS-fingerprint grounds; curl_cffi
# impersonates a real browser's TLS handshake to get past it. Not used by any other
# source, including Stith, which requests already handles fine.
curl_cffi==0.16.0

# Testing
pytest>=8.0

# Code quality
ruff>=0.5
black>=24.0
